#!/bin/sh
set -eu

PROJECT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PYTHON="$PROJECT_DIR/env/bin/python3"

if [ ! -x "$PYTHON" ]; then
    PYTHON=python3
fi

cd "$PROJECT_DIR"
exec "$PYTHON" -m station_director "$@"
