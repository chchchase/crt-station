# Install separately, without overwriting an existing drop-in.
# Persistent on disk: a crash or reboot must not clear this inhibition.
[Unit]
ConditionPathExists=!%h/FieldStation42/runtime/director/application-maintenance
